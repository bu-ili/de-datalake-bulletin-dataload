apiVersion: v1
kind: ConfigMap
metadata:
  name: bulletin-dataload-config
  namespace: dagster
  labels:
    app: bulletin-dataload
    component: code-location
data:
  # Configuration file path (optional - defaults to /app/config/config.json)
  # All other configuration values are now in config.json
  CONFIG_PATH: "/app/config/config.json"
